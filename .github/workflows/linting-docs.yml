name: Linting (Docs)
on: push
jobs:
  docs:
    runs-on: ubuntu-latest
    steps:
      -   name: Checkout
          uses: actions/checkout@v2
      -   name: Lint Markdown docs
          run: yarn && yarn docs:lint
